import { Routes } from '@angular/router';
import { BoosterCardsComponent } from './booster-cards/booster-cards.component';

export const routes: Routes = [
  {path: "cards", component: BoosterCardsComponent}
];
